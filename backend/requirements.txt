# 后端依赖(LangGraph + CopilotKit + FastAPI + Claude)
# Python 3.12(本机用 /opt/homebrew/bin/python3.12 创建 venv)

# —— 任务契约点名的 5 个 ——
langgraph>=1.2.10
copilotkit>=0.1.94
fastapi>=0.141.1
uvicorn[standard]>=0.52.1
anthropic>=0.120.2

# —— 实际功能必需(create_agent / Claude / AG-UI 端点挂载)——
# copilotkit 依赖 ag-ui-langgraph,但这里显式列出以保证 add_langgraph_fastapi_endpoint 可用
langchain>=1.3.14
langchain-anthropic>=1.5.4
ag-ui-langgraph>=0.0.42

# —— 读取 .env ——
python-dotenv>=1.0.1

# —— 会话持久化(LangGraph checkpointer → Supabase Postgres)——
# 让对话/PRD/代码/预览状态跨刷新与容器重启存活(DATABASE_URL 为空时回落内存 saver)。
# psycopg3 + 连接池;AsyncPostgresSaver 在 lifespan 里 open + setup。
langgraph-checkpoint-postgres>=2.0.10
psycopg[binary]>=3.2,<4
psycopg-pool>=1.1
