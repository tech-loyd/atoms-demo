// atoms starter PostCSS 配置(Tailwind 3.x + autoprefixer)。
// .cjs 后缀:package.json 是 type:module,用 .cjs 强制 CommonJS(require)。
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
